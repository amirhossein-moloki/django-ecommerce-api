# docker-compose.yml (برای محیط توسعه)
# این فایل اصلی برای راه‌اندازی محیط توسعه با hot-reloading است.
# برای اجرا از دستور `docker compose up --build` استفاده کنید.

version: '3.8'

services:
  web:
    build:
      context: .
      args:
        - DEV=true
    image: ecommerce-api-dev:latest
    container_name: ecommerce-backend-dev
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
      - redis
    networks:
      - default
      - monitoring_net

  db:
    image: postgres:15-alpine
    container_name: ecommerce-postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    container_name: ecommerce-redis

  celery_worker:
    build:
      context: .
      args:
        - DEV=true
    image: ecommerce-api-dev:latest
    container_name: ecommerce-celery-worker-dev
    command: celery -A ecommerce_api worker -l info
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - redis
      - db

  celery_beat:
    build:
      context: .
      args:
        - DEV=true
    image: ecommerce-api-dev:latest
    container_name: ecommerce-celery-beat-dev
    command: celery -A ecommerce_api beat -l info
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - redis
      - db

  flower:
    build:
      context: .
      args:
        - DEV=true
    image: ecommerce-api-dev:latest
    container_name: ecommerce-flower-dev
    command: celery -A ecommerce_api flower --broker=${CELERY_BROKER_URL}
    ports:
      - "5555:5555"
    env_file:
      - .env
    depends_on:
      - redis
      - celery_worker

volumes:
  postgres_data:

networks:
  monitoring_net:
    external: true
