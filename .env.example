# -----------------------------------------------------------------------------
# D J A N G O  -  C O R E
# -----------------------------------------------------------------------------
# The Django settings module to use.
# For development: ecommerce_api.settings.development
# For production: ecommerce_api.settings.production
DJANGO_SETTINGS_MODULE='ecommerce_api.settings.production'

# A secret key for a particular Django installation. This is used to provide cryptographic signing.
# It is REQUIRED for production. Generate a new key here: https://djecrety.ir/
SECRET_KEY='django-insecure-your-super-secret-key'

# Controls whether the application is in debug mode.
# !! IMPORTANT !! Set to False in production for security and performance.
DEBUG=False

# A comma-separated list of host/domain names that this Django site can serve.
# !! IMPORTANT !! In production, this must be set to your actual domain name(s).
# Using '*' is insecure and not recommended for production.
# Example: ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------------------------------------------------------
# D A T A B A S E   &   C A C H E
# -----------------------------------------------------------------------------
# URL for connecting to the PostgreSQL database.
# Format: postgres://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL='postgres://user:password@database:5432/db'

# These variables are used by the official Postgres Docker image to initialize the database.
# They should match the credentials in DATABASE_URL.
POSTGRES_DB=db
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# URL for connecting to Redis. Used for caching and Celery.
# Format: redis://<host>:<port>/<db_number>
REDIS_URL='redis://cache:6379/1'

# -----------------------------------------------------------------------------
# S E C U R I T Y  -  C O R S   &   C S R F
# -----------------------------------------------------------------------------
# A comma-separated list of origins that are authorized to make cross-site HTTP requests.
# Example: CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://yourfrontend.com
CORS_ALLOWED_ORIGINS=

# A comma-separated list of trusted origins for CSRF purposes (e.g., for secure POST forms).
# Example: CSRF_TRUSTED_ORIGINS=http://localhost:3000,https://yourfrontend.com
CSRF_TRUSTED_ORIGINS=

# Whether to allow credentials (cookies, auth headers) in cross-origin requests.
CORS_ALLOW_CREDENTIALS=True

# -----------------------------------------------------------------------------
# E M A I L
# -----------------------------------------------------------------------------
# URL for configuring email sending.
# For development, 'consolemail://' prints emails to the console.
# For production, use a real SMTP server. Example: 'smtp://user:pass@smtp.example.com:587'
EMAIL_URL='consolemail://'

# -----------------------------------------------------------------------------
# S I T E   &   A D M I N S
# -----------------------------------------------------------------------------
# The domain of your site. Used for generating absolute URLs.
DOMAIN='localhost:8000'

# The name of your site.
SITE_NAME='Hypex'

# A comma-separated list of site admins in the format 'Name:email'.
# Example: ADMINS='John Doe:john@example.com,Jane Doe:jane@example.com'
ADMINS=

# -----------------------------------------------------------------------------
# C E L E R Y
# -----------------------------------------------------------------------------
# URL for the Celery message broker (uses Redis).
CELERY_BROKER_URL='redis://cache:6379/0'

# URL for the Celery result backend (uses Redis).
CELERY_RESULT_BACKEND='redis://cache:6379/0'

# Interval in seconds for the 'cancel-pending-orders' task.
CANCEL_PENDING_ORDERS_INTERVAL=600.0

# -----------------------------------------------------------------------------
# J W T   A U T H E N T I C A T I O N
# -----------------------------------------------------------------------------
# Lifetime of the JWT access token in days.
JWT_ACCESS_TOKEN_LIFETIME_DAYS=1

# Lifetime of the JWT refresh token in days.
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# -----------------------------------------------------------------------------
# S O C I A L   A U T H ( G O O G L E )
# -----------------------------------------------------------------------------
# Google OAuth2 credentials for social login. REQUIRED if using Google login.
# Obtain these from the Google API Console: https://console.developers.google.com/
GOOGLE_OAUTH2_KEY=
GOOGLE_OAUTH2_SECRET=

# -----------------------------------------------------------------------------
# T H I R D - P A R T Y   S E R V I C E S
# -----------------------------------------------------------------------------

# --- SMS.ir ---
# Template ID for sending OTP messages via sms.ir. REQUIRED for OTP functionality.
SMS_IR_OTP_TEMPLATE_ID=

# --- Postex (Shipping) ---
# Details for the sender to be used with the Postex shipping service.
POSTEX_SENDER_NAME='Your Company Name'
POSTEX_SENDER_PHONE='Your Company Phone'
POSTEX_SENDER_ADDRESS='Your Company Address'
POSTEX_SENDER_POSTAL_CODE='Your Company Postal Code'
POSTEX_FROM_CITY_CODE=1
POSTEX_SERVICE_TYPE='standard'

# --- Zibal (Payment Gateway) ---
# Your merchant ID for the Zibal payment gateway. REQUIRED for payments.
ZIBAL_MERCHANT_ID=
# Webhook secret for verifying callbacks from Zibal. REQUIRED for payments.
ZIBAL_WEBHOOK_SECRET=
# Comma-separated list of allowed IP addresses for Zibal webhooks.
ZIBAL_ALLOWED_IPS=127.0.0.1

# -----------------------------------------------------------------------------
# O B S E R V A B I L I T Y   &   D E B U G G I N G
# -----------------------------------------------------------------------------
# Service name for OpenTelemetry.
OTEL_SERVICE_NAME='ecommerce-api'

# OTLP endpoint for sending traces. Leave empty to disable.
# Example for Jaeger running locally: http://localhost:4318
OTEL_EXPORTER_OTLP_ENDPOINT='http://jaeger:4318'

# Comma-separated list of IPs that can see the Django Debug Toolbar.
INTERNAL_IPS=127.0.0.1,localhost
